# Database
# In dev locale: avvia Postgres con `docker compose up -d`
DATABASE_URL="postgresql://lume:lume_dev_password@localhost:5433/lume_dev?schema=public"

# NextAuth v5
# Genera con: openssl rand -base64 32
AUTH_SECRET=""
AUTH_URL="http://localhost:3000"

# Email magic link (opzionale in v0.1, attivato in sessione 2)
EMAIL_SERVER_HOST=""
EMAIL_SERVER_PORT=""
EMAIL_SERVER_USER=""
EMAIL_SERVER_PASSWORD=""
EMAIL_FROM=""

# Versione consenso corrente. Ogni cambio richiede ri-firma utenti esistenti.
NEXT_PUBLIC_CONSENT_VERSION="2026-05-01"

# App version, esposta in AssessmentSession.appVersion per riproducibilita
NEXT_PUBLIC_APP_VERSION="0.1.0"

# Sentry (sessione 9)
SENTRY_DSN=""
SENTRY_AUTH_TOKEN=""

# Plausible (sessione 9)
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=""

# Anthropic Claude API (post-MVP, restituzioni narrative AI-assistite)
ANTHROPIC_API_KEY=""
